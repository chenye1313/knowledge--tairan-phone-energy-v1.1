# 上传前检查清单

## 当前状态

- 本目录是本地备份和 Monica 待导入源。
- 尚未上传 Monica。
- 已上传 GitHub：`https://github.com/chenye1313/knowledge--tairan-phone-energy-v1.1`。
- 尚未覆盖旧版 `knowledge--tairan-phone-energy`。

## Monica 上传前

- 确认 Monica 是否接受中文文件名。
- 确认 Monica 是否接受 `raw/` 和 PDF。
- 确认是否需要把 `raw/` 作为低权重资料或只保留在 GitHub。
- 确认外部八字工具/联网能力在目标平台是否可用。
- 确认 `monica.config.json` 中的 `repo_owner`、`repo_name`、`branch` 是否符合最终 GitHub 仓库。

## GitHub 上传前

- 已删除/排除 `.DS_Store`，本轮扫描未发现。
- 已跑 JSON 校验。
- 已跑固定规则校验。
- 已检查是否包含隐私信息或凭证，未发现 `.env`、私钥名、`token` 或 `secret` 文件。
- 已新建公开仓库 `knowledge--tairan-phone-energy-v1.1`。

## Monica 导入文件

- Schema：`schema.json`
- Prompt Patch：`prompt_patch.txt`
- 部署速查卡：`deployment_card.md`
- GitHub Raw Base：`https://raw.githubusercontent.com/chenye1313/knowledge--tairan-phone-energy-v1.1/main`

## 验证命令

```bash
python3 tools/phone_energy_pai_pan.py --verify-rules --compact
python3 -m json.tool phone_energy_fixed_rules.json >/dev/null
python3 -m json.tool phone_energy_selection_rules.json >/dev/null
python3 -m json.tool tools/phone_energy_fixed_rules.json >/dev/null
python3 -m json.tool tools/phone_energy_selection_rules.json >/dev/null
find . -name '.DS_Store' -print
```

## 不得声明

- 不得说 Monica 已上线，除非有 Monica 上传成功证据。
- 可以说 GitHub 已推送，但必须同时给出 GitHub 仓库 URL、push 输出或 raw URL 验证证据。
- 不得说已内置八字排盘脚本；当前只是定义联网/外部工具契约。
